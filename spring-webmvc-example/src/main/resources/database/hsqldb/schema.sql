CREATE TABLE pet (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    name VARCHAR(30),
    owner VARCHAR(60),
    species VARCHAR(90),
    sex CHAR(1),
    birth DATE,
    death DATE
);